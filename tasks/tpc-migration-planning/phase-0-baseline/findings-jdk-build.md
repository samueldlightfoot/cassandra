# Explorer report — JDK / build constraints (2026-07-05)

Repo: `/Users/samlightfoot/repos/fork/cassandra`, branch `flush-write-pacing` (trunk-based).

## 1. Supported / required JDK versions

- Canonical declaration in `build.xml:47-48`: `java.default` = **11** (`build.xml:47`),
  `java.supported` = **`11,17,21`** (`build.xml:48`).
- Build fails on any JDK not in that list — `build.xml:225-227` (`<fail message="Unsupported JDK version used...">`
  guarded by `<contains string="${java.supported}">`).
- javac source/target pinned to the running JDK; lower JDKs explicitly unsupported at runtime —
  comment `build.xml:44`. Non-default JDKs (17, 21) labelled "experimental / development and testing" — `build.xml:43`.
- CI derives its JDK matrix from these two properties — `.jenkins/Jenkinsfile:239-242`
  (`extractBuildXmlProperty(build_xml, 'java.default')` and `'java.supported').split(',')`).
- Per-JDK config exists only up to 21: `conf/jvm11-server.options`, `conf/jvm17-server.options`,
  `conf/jvm21-server.options` (and matching `-clients`); ant arg-item resource sets for exactly
  jvm11/jvm17/jvm21 — `build.xml:258, 295, 339`.

**Minimum supported JDK: 11 (default). Maximum: 21. No JDK 22+ support on this branch.**

**Panama consequence:** max supported JDK is 21, where `java.lang.foreign` (FFM) is a **preview**
API (final only in 22+). So FFM is NOT usable without `--enable-preview` (which forces
source/target to the exact JDK — conflicts with the multi-JDK build). No `--enable-preview`,
`--enable-native-access`, or `foreign` flags exist in any `conf/jvm*-server.options`.

## 2. Existing java.lang.foreign / Panama / MemorySegment usage

**Zero hits** for `java.lang.foreign`, `MemorySegment`, `Linker.nativeLinker`,
`FunctionDescriptor`, `SymbolLookup` across `src/` and `test/`.

`MethodHandles` appears only for reflection/UDF plumbing, not native FFI:
`JavaDriverUtils.java:21-22,46`; `JavaBasedUDFunction.java:24-25,353`.

## 3. JNA usage and version

- **JNA 5.13.0** (jna + jna-platform): `.build/parent-maven-pom.xml:555-565`; runtime dep in
  `.build/cassandra-deps-maven-pom.xml:157-158, 219-220`. Jars: `lib/jna-5.13.0.jar`,
  `lib/jna-platform-5.13.0.jar` (gitignored resolver output).
- Loading idiom (`Native.register` direct mapping against libc): platform dispatch
  `NativeLibrary.java:103-107` → `NativeLibraryDarwin`/`NativeLibraryLinux` into
  `NativeLibraryWrapper wrappedLibrary` (`:80`). Linux binding `NativeLibraryLinux.java:23-25,56`
  — `Native.register(com.sun.jna.NativeLibrary.getInstance("c", Collections.emptyMap()))`.
  Darwin same pattern `NativeLibraryDarwin.java:23-25,56`. Errors via
  `com.sun.jna.LastErrorException` (`NativeLibrary.java:158,199,273,...`).
- Off-heap malloc/free via JNA: `MemoryUtil.java:25,87,92` (`Native.malloc/free`).
- JNA not referenced in build.xml — purely Maven-resolved.
- Related FFI already on classpath (transitive via Chronicle `posix`): **jnr-ffi 2.2.13**
  (`.build/parent-maven-pom.xml:567-569`), `lib/jffi-1.3.11.jar` + `-native.jar`, `lib/jnr-*`.

## 4. Unsafe / VarHandle / memory-ordering idioms

- **No explicit fences and no VarHandle anywhere**: grep for
  `loadFence|storeFence|fullFence|acquireFence|releaseFence|VarHandle` over `src/`+`test/` = 0 matches.
- `sun.misc.Unsafe` used for raw off-heap plain load/store: `MemoryUtil.java:30,37,51-53,82,97,105-107,180`;
  `Memory.java:30,38,43-45`; also `FastByteOperations.java`, `concurrent/Ref.java`, SAI disk readers.
- Established ordering idiom = `Atomic*FieldUpdater.lazySet` (release-store) + CAS:
  `ShareableBytes.java:106,132`; `AbstractFuture.java:110,120,131,137`; `SEPWorker.java:139,165,169,215`;
  `IntrusiveStack.java:128,134`.

Takeaway: io_uring ring acquire/release would introduce a new idiom; closest existing pattern
is lazySet for release. (Phase 0 D2 pins Unsafe getIntVolatile/putOrderedInt.)

## 5. How third-party dependencies get added

- **`lib/` is resolver output, not checked in**: `.gitignore:16` ignores `lib/`; allowlist
  exceptions only (`.gitignore:20-22`). 0 tracked jars (`git ls-files lib/ | grep .jar` empty).
- Deps declared in `.build/cassandra-deps-maven-pom.xml` (runtime) with versions in
  `.build/parent-maven-pom.xml`; fetched by resolver ant tasks in `.build/build-resolver.xml`
  (`resolver-retrieve-build` `:181`, `resolver-dist-lib` `:209`), imported at `build.xml:2370`.
- No `lib/licenses/` dir. Compliance: Apache RAT (`.build/build-rat.xml`, fails at `:112`;
  covered types in `doc/modules/cassandra/assets/license_compliance.rst:33`), top-level
  LICENSE.txt/NOTICE.txt, CircleCI `license` workflow (`.circleci/license.yml`).
- Security gates: OWASP dependency-check (`.build/build-owasp.xml`, 12.1.6), Snyk (`.snyk`,
  generated by `.build/generate-snyk-file`).

## 6. Native code in-tree?

**None.** No `native/` dir, no `.c`/`.h`/`.so` sources, no `<cc>`/gcc/javah tasks in build.xml.
All native access is JNA direct mapping + prebuilt native jars from deps (netty epoll, jffi,
chronicle). No pattern exists for compiling/shipping project-owned native code.
